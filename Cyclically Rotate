if (n <= 1) // If array has 0 or 1 element, nothing to rotate
        return;

    int last = arr[n - 1]; // Store the last element
    for (int i = n - 1; i > 0; i--) {
        arr[i] = arr[i - 1]; // Shift elements to the right
    }
    arr[0] = last;
